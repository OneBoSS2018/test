

<p>
  <strong>Title:</strong>
  <%= @post.title %>
</p>

<p>
  <strong>User:</strong>
  <%= @post.user.username %>
</p>
<% if current_user && current_user.admin %>
<%= link_to 'Edit', edit_post_path(@post) %> <% end %>|
<%= link_to 'Back', posts_path %>
